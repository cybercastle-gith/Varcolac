/** Forçar roles manualmente, para reproduzir um caso específico sem sortear até dar certo. */
export function ScenarioPanel() {
  // TODO
  return (
    <section>
      <h2 className="text-neutral-400 uppercase tracking-widest text-xs mb-2">ScenarioPanel</h2>
      <p className="text-neutral-500">TODO — Forçar roles manualmente, para reproduzir um caso específico sem sortear até dar certo.</p>
    </section>
  );
}
