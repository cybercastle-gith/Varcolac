/** Estado interno completo: quem está protegido, bloqueado, perfurado, preso, marcado, com estertor pendente. Tudo que no app é invisível por design. */
export function StatePanel() {
  // TODO
  return (
    <section>
      <h2 className="text-neutral-400 uppercase tracking-widest text-xs mb-2">StatePanel</h2>
      <p className="text-neutral-500">TODO — Estado interno completo: quem está protegido, bloqueado, perfurado, preso, marcado, com estertor pendente. Tudo que no app é invisível por design.</p>
    </section>
  );
}
